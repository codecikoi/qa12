// Landing page — for now re-exports the static HTML landing
// Will be replaced with full Next.js components in next iteration

export default function HomePage() {
  return (
    <main>
      {/* Landing sections will go here */}
      {/* See: src/components/landing/ */}
      <div style={{ padding: '40px', textAlign: 'center', color: '#00e5ff', fontFamily: 'monospace' }}>
        QA12 — Landing page coming soon
      </div>
    </main>
  )
}
